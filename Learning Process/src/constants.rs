fn main() {
    // this below is a constant, it is basically a variable but the value of it cannot be changed, however you try to modify it will fail and won't work.
    const SECONDS_IN_MINUTES: u32 = 60;
    println!("Seconds in each minute: {}", SECONDS_IN_MINUTES);
}
